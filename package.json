{
  "name": "root-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && yarn preview --host 0.0.0.0 --port ${PORT:-4173}",
    "build": "cd frontend && yarn install && yarn build"
  }
}
